# Bytecode Notes

Running notes from Phase 0 — one entry per surprise or insight.
The goal: read any small method's disassembly and roughly predict it before `javap`.

---

## Method set

- [ ] `int add(int a, int b)` — baseline
- [ ] `for` loop summing 1..n — jumps/labels
- [ ] `if/else` returning different ints — branching
- [ ] method calling another method — `invokevirtual` + descriptors
- [ ] `String greet(String name)` — string concatenation surprise
- [ ] `try/finally` — exception table surprise

---

## Notes

<!-- Add one entry per method or surprise below. -->

